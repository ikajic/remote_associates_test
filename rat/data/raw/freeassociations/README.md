#### process_data.py
A script used to read the data in `./data`. Produces a Python `pickle` file
`free_association_vocabulary` stored in the `processed` directory.

#### read_data.py:
A script containing a function to read the processed data
(`free_association_vocabulary`) stored in `../../processed` directory. 
Used by the model.

